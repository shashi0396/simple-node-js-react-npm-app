pipeline {
    agent any

    stages {
        stage('Pull and Build Images') {
            steps {
                sh 'docker-compose pull'
                sh 'docker-compose build'
            }
        }
    }
}
